{
  "name": "pxy-monorepo",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev -w web\" \"npm run dev -w admin\"",
    "build": "npm run build -w web && npm run build -w admin"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
