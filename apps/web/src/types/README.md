# types/

Folder ini untuk TypeScript type definitions dan interfaces lokal `apps/web`.

Contoh penggunaan:
- Types untuk API response spesifik web app
- Types untuk state management lokal
- Types yang belum perlu dishare ke package lain

Untuk types yang dipakai lintas package, tambahkan ke `packages/core/`.
